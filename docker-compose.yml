version: "2"

services:
  dev_links_info_crosver_api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: npm run dev
    ports:
      - 8081:8081
      - 9229:9229
    volumes:
      - .:/usr/srv/links_info_crosver_api
